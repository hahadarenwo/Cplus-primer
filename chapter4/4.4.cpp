#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main5()
{
	//4.13
	//a i = 3 d = 3.0
	//b d = 3.5 i =3

	//4.14
	// 编译错误。赋值运算符左侧必须是一个可修改的左值。而字面值是右值。
	// if (i = 42)   // true.

	//4.15
	//指针不能赋值给int
	//dval = ival = 0;
	//pi = 0;


	//4.16
	//判断总为true
	/*f((p = getPtr()) != 0)
	  if (i == 1024)*/
	return 0;
}