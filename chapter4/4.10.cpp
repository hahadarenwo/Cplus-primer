#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main11()
{
	//4.31
	//在4.5节（132页）已经说过了，除非必须，否则不用递增递减运算符的后置版本。在这里要使用后者版本的递增递减运算符不需要任何改动。

	//4.32
	//这个循环在遍历数组 ia，指针 ptr 和 整型 ix 都是起到一个循环计数的功能。

	//4.33
	//someValue为真 存在xy+1 不然-1
	return 0;
}