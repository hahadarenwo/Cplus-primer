#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main7()
{
	//4.20
	//合法 返回迭代器所指向的元素，然后迭代器递增。
	//不合法。因为vector元素类型是 string，没有 ++ 操作。
	//不合法。这里应该加括号。
	//合法。判断迭代器当前的元素是否为空。
	//不合法。string 类型没有 ++ 操作。
	// 合法。判断迭代器当前元素是否为空，然后迭代器递增。


	return 0;
}