#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main1()
{
	//5.1 只含义一个单独的分号的语句是空语句。如：;
	//如果在程序的某个地方，语法上需要一条语句但是逻辑上不需要，此时应该使用空语句。

	//5.2 {}包裹起来的叫做块
	//如果在程序的某个地方，语法上需要一条语句，而逻辑上需要多条语句，此时应该使用块

	//5.3
	/*while (val <= 10)
		sum += val, ++val;*/
	//可读性降低了

	return 0;
}